# Anthropic API key (https://console.anthropic.com/)
ANTHROPIC_API_KEY=
# Password visitors must enter to use the agent (protects your API credits).
# Separate several codes with commas, e.g. ownercode,friendcode
ACCESS_CODE=
